--Preview

SELECT 
    IncidentId,
    IncidentTitle,
    IncidentCreatedDate,
    TokenSeverity,
    CONCAT(
        'System detected a ',
        TokenSeverity,
        ' severity event requiring triage.'
    ) AS NewDescriptionPreview
FROM Incidents
ORDER BY IncidentId;



-- Update

